# My neovim setup

## How to use

1. First, install [neovim](https://neovim.io/).
2. Then, locate the `nvim/` directory that was created during installation.
3. Replace the contents of the `nvim/` directory with those of this repository.

